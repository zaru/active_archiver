require "spec_helper"

describe ActiveArchiver do
  it "has a version number" do
    expect(ActiveArchiver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
